<?xml version="1.0" encoding="utf-8"?>
<!-- Маленький виджет: две клетки на две, помещается на любой рабочий стол.

     Здесь только то, ради чего на виджет смотрят мельком: серия, одно слово и
     сам Читавук. Списка слов нет намеренно — на двух клетках он превратится в
     нечитаемые обрезки.

     Читавук сидит в углу подложкой, а подписи идут поверх: столбцом рядом с
     ним на 110dp ширины текст остался бы в три буквы. Правый отступ подписей
     больше ширины фигуры, поэтому строки на неё не наезжают, а длинные
     обрезаются многоточием. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:id="@+id/widget_root">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_gravity="top"
        android:layout_marginTop="6dp"
        android:layout_marginStart="6dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/widget_garland_tile"
        android:contentDescription="@null" />

    <ImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="2dp"
        android:layout_marginBottom="2dp"
        android:src="@drawable/citavuk_widget"
        android:contentDescription="@string/widget_daily_title" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="28dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/widget_stove"
                android:contentDescription="@null" />
            <TextView
                android:id="@+id/widget_streak"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/widget_accent" />
        </LinearLayout>

        <!-- Отступ справа растёт сверху вниз: строки ближе к низу идут вровень
             с Читавуком, а верхние — над ним, и ужимать их незачем. -->
        <TextView
            android:id="@+id/widget_stats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:layout_marginEnd="28dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="11sp"
            android:textColor="@color/widget_ink_soft" />

        <ProgressBar
            android:id="@+id/widget_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="5dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="40dp"
            android:max="100"
            android:progressDrawable="@drawable/widget_progress" />

        <TextView
            android:id="@+id/widget_word_0"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="9dp"
            android:layout_marginEnd="56dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/widget_ink" />

        <TextView
            android:id="@+id/widget_word_tr_0"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="56dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="11sp"
            android:textColor="@color/widget_ink_soft" />
    </LinearLayout>
</FrameLayout>
